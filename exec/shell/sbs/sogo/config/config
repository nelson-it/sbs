postfixconf=/etc/postfix
dovecotconf=/etc/dovecot

sogoconfigdir=$configdir/sogo
dovecotconfigdir=$configdir/dovecot
postfixconfigdir=$configdir/postfix

[ ! "$apachehost" = "" ] || apachehost=$(hostname)
